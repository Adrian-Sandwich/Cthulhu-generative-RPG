[pytest]
# Only tests/ holds the real suite. game/ and tools/ carry standalone scripts
# (some named test_*.py) that use flat imports and break bare collection —
# see docs/TECH_DEBT.md ("Dead standalone scripts").
testpaths = tests
