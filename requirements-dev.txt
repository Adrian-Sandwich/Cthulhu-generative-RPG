# Development / CI dependencies. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
pytest>=7.0.0
pytest-subtests>=0.11.0

# Narrow static gate — see pyrightconfig.json (needs node, same as the
# frontend checks).
pyright

# Isolated HTTP load harness on Windows/Linux; not production dependencies.
waitress>=3,<4
psutil>=6,<8
